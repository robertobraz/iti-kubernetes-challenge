apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iti-kotlin-chart.fullname" . }}-test-connection"
  labels:
    {{- include "iti-kotlin-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iti-kotlin-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
