apiVersion: v1
kind: Service
metadata:
  name: {{ include "iti-kotlin-chart.fullname" . }}
  labels:
    {{- include "iti-kotlin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iti-kotlin-chart.selectorLabels" . | nindent 4 }}
